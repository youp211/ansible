[gaming]
localhost ansible_connection=local

[gaming:vars]
# The desktop user whose Sunshine config, autostart entries and nice rlimit are
# managed. Set this before the first run.
target_user=youruser
