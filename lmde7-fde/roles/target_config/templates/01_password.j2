#!/bin/sh
# managed by lmde7-fde finalize — GRUB menu-edit protection
# NOTE: with superusers set, unmodified menu entries require this password to
# BOOT as well unless marked --unrestricted. See README "GRUB password" before
# enabling on an unattended machine.
cat <<EOF
set superusers="{{ grub_superuser }}"
password_pbkdf2 {{ grub_superuser }} {{ grub_password_pbkdf2 }}
EOF
